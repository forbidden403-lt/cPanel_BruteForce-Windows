# cPanel Brute Force

@author => Forbidden_403

Download;
  git clone https://github.com/forbidden403-lt/cPanel_BruteForce-Windows.git
  cd cPanel_BruteForce-Windows
  python -m pip install requests
  pip install selenium
  pip install webdriver-manager
  python cpanel_brute.py

![image](https://berita-terkini.id/images/cPanel-Brute-Force-Windows.png)
